apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: reondev-top-tls
  namespace: puresecure
spec:
  secretName: reondev-top-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - reondev.top
    - grafana.reondev.top
