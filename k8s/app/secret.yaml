# Placeholder — do NOT put real secrets here.
# Create the secret imperatively using the setup script:
#
#   kubectl create secret generic app-secrets \
#     --namespace=puresecure \
#     --from-literal=SERVICE_API_KEY="your-api-key" \
#     --from-literal=GF_ADMIN_PASSWORD="your-grafana-password"
#
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: puresecure
  labels:
    app: cwe-explorer
type: Opaque
data: {}
