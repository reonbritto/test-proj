# Python
__pycache__/
*.py[cod]
*.egg-info/
venv/
.venv/

# IDE & Tools
.vscode/
.idea/
.claude/
.gemini/

# Claude Code local settings (per-machine, not project config)
.claude/settings.local.json

# Cache & Runtime Data — exclude large/generated files, keep the placeholder
data/*.xml
data/*.xml.zip
data/*.json

# Test & Coverage
.pytest_cache/
coverage-html/
coverage.xml
test-results.xml
htmlcov/
.coverage

# Reports
flake8-report.txt
bandit-report.json
pip-audit-report.json
safety-report.json

# Build
_site/

# OS
.DS_Store
Thumbs.db

# Environment
.env
*.pem
*.key

# Frontend
frontend/node_modules/
frontend/dist/

# Terraform — ignore state and secrets, but KEEP the lockfile (it pins provider versions)
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/terraform.tfvars
terraform/*.auto.tfvars

# Monitoring & data volume data (docker-compose named volumes written to host)
redis-data/
prometheus-data/
grafana-data/
alertmanager-data/
/docs
/todo.md
/DEPLOYMENT.md
/terraform/.terraform.lock.hcl
/README-no-images.md